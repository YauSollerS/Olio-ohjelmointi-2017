<Window x:Class="HT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:HT.ViewModels"
        xmlns:views="clr-namespace:HT.Views"
        xmlns:local="clr-namespace:HT"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="925">

    <Window.Resources>
        <DataTemplate x:Name="MerchandiseView" DataType="{x:Type viewmodels:TuotteetViewModel}">
            <views:TuotteetView DataContext="{Binding}"/>
        </DataTemplate>

    </Window.Resources>

    <DockPanel>
        <Grid DockPanel.Dock="Left" MinWidth="125" Background="#FFFDFDFD">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                <Label x:Name="clockLabel" FontSize="22" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.RowSpan="4" Grid.Column="0">
                <Button x:Name="btnTuotteet" Content="Tuotteet" Height="50" Margin="2" Click="btnTuotteet_Click" />
                <Button x:Name="btnMyynti" Content="Myynti" Height="50" Margin="2" Click="btnMyynti_Click" />
                <Button x:Name="btnHistoria" Content="Historia" Height="50" Margin="2" />
                <Button x:Name="btnToimittajat" Content="Toimittajat" Height="50" Margin="2" />
            </StackPanel>
        </Grid>

        <ContentControl Content="{Binding}"></ContentControl>
    </DockPanel>
</Window>
