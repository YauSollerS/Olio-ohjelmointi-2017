<UserControl x:Class="HT.Views.HistoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HT.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid Background="#EBF4FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="3">
            <TextBlock Text="HISTORIA" FontSize="26" HorizontalAlignment="Center" DockPanel.Dock="Top"/>
            <ListView ItemsSource="{Binding Liiketoimet}" SelectionChanged="Liiketoimi_Selected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="2" BorderThickness="0,0,0,1" BorderBrush="Black">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <DockPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Width="50" Text="{Binding Id}"/>
                                    <StackPanel Margin="5">
                                        <TextBlock Text="{Binding AsiakasNimi}" FontSize="16" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding ShortDate}" FontSize="16"/>
                                        <TextBlock Text="{Binding Yhteensa, StringFormat=€{0}}" FontSize="16"/>
                                    </StackPanel>
                                </DockPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>

        <DockPanel Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2" Grid.RowSpan="2">
            <Grid Margin="0,36,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                    <TextBlock Text="Liiketoimi ID"/>
                    <TextBox x:Name="liiketoimiIdBox" Text="{Binding SelectedLiiketoimi.Id}" IsReadOnly="True" />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                    <TextBlock Text="Ostopäivä"/>
                    <TextBox x:Name="paivaBox" Text="{Binding SelectedLiiketoimi.ShortDate}" IsReadOnly="True" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="5">
                    <TextBlock Text="Maksutapa"/>
                    <TextBox x:Name="maksutapaBox" Text="{Binding SelectedLiiketoimi.Maksutapa}" IsReadOnly="True"  />
                </StackPanel>
                
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                    <TextBlock Text="Nimi"/>
                    <TextBox x:Name="nimiBox" Text="{Binding SelectedLiiketoimi.AsiakasNimi}" IsReadOnly="True"  />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                    <TextBlock Text="Sähköposti"/>
                    <TextBox Text="{Binding SelectedLiiketoimi.AsiakasSposti}" IsReadOnly="True" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                    <TextBlock Text="Puhelin"/>
                    <TextBox x:Name="puhelinBox" />
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="0" Margin="5">
                    <TextBlock Text="Laskutusosoite"/>
                    <TextBox x:Name="laskutusosoiteBox" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="5">
                    <TextBlock Text="Toimitusosoite"/>
                    <TextBox x:Name="toimitusosoiteBox" />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="2" Margin="5">
                    <TextBlock Text="Tuottaja"/>
                    <TextBox x:Name="tuottajaBox" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="2" Margin="5">
                    <TextBlock Text="Seurantanumero"/>
                    <TextBox x:Name="seurantanumeroBox" />
                </StackPanel>
            </Grid>
        </DockPanel>

    </Grid>
</UserControl>
